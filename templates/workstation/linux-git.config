# Reviewed linux-git build overlay.  The pinned AUR recipe appends this file to
# its generated config before `make olddefconfig`; do not put user-space CFLAGS
# or makepkg settings here.
CONFIG_X86_NATIVE_CPU=y
