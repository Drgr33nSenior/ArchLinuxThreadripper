# Copy to ignored inventory.local.ini. No target is configured by default.
[ai_lab_baremetal]
# chosen-alias ansible_host=REPLACE_PRIVATE_ADDRESS ansible_user=REPLACE_ADMIN

[ai_lab_baremetal:vars]
lab_environment=dev
