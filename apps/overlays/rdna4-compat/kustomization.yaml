apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Diagnostic opt-in, not evidence that these workarounds are required or safe.
resources: [../single-gpu]
patches:
  - path: compatibility.yaml
